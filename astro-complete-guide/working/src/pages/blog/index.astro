---
type Post = {
  userId: number
  id: number
  title: string
  body: string
}

type Props = {
  posts: Post[]
}

// export async function getStaticPaths() {
//   const response = await fetch("https://jsonplaceholder.typicode.com/posts")
//   const posts = (await response.json()) as Post[]

//   return posts
// }

const { posts } = Astro.props
const mds = await Astro.glob("../mds/*.md")
---

<h1>Blog</h1>

{
  mds.map(post => <div>
    <a href={post.url} >
      {post.frontmatter.title}
      </a>
  </div>)
}

<!-- 
{
  posts.map(post => (
    <>
      <h3>{post.title}</h3>
      <div>{post.body}</div>
    </>
  ))
} -->
