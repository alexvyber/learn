import { WrapResponeInterceptor } from './wrap-respone.interceptor';

describe('WrapResponeInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapResponeInterceptor()).toBeDefined();
  });
});
